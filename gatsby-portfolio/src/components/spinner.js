import React from 'react';
import './spinner.css'

const Spinner = ()=> {
    return(
        <div id="Spinner">
            <div className="lds-dual-ring"></div>
        </div>
    );
}

export default Spinner;